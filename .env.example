# Frontend -> backend API
# true: call local MVP backend API
# false: local mock-only mode (no backend/FASHN)
# Netlify preview without a deployed API can omit VITE_BACKEND_BASE_URL
# and the app will fall back to local mock mode automatically.
VITE_USE_BACKEND_API=true
VITE_BACKEND_BASE_URL=http://localhost:8787

# Backend runtime env (set in shell when starting `npm run api`)
# export FASHN_API_KEY=xxxx
# export FASHN_BASE_URL=https://api.fashn.ai/v1
# export API_PORT=8787
# export SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# export SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
# export SUPABASE_STORAGE_BUCKET=app-assets
